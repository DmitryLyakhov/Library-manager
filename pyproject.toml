[project]
name = "library-manager"
version = "0.1"
dependencies = ["openpyxl==3.1.3", "rich==14.1.0"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project.scripts]
library-manager = "library_manager.cli_interface:main"
